# Tuỳ chọn — chỉ cần nếu muốn lưu Sổ lãi (/profit) dài hạn qua Supabase.
# Xem hướng dẫn tạo project + chạy SQL trong README.md.
# Không điền thì app tự dùng localStorage, mọi thứ vẫn chạy bình thường.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
