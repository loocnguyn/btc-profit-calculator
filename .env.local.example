# Required — see the "Supabase setup" section in README.md.
# The app will not function without these; accounts and all data
# are stored in Supabase (no more anonymous/localStorage mode).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
